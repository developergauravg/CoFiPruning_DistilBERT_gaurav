sst2:
  hf_dataset: glue
  hf_subset: sst2
  text_fields: [sentence]
  label_field: label
  num_labels: 2
  train_split: train
  eval_split: validation
  max_length: 128
  train_batch_size: 16
  eval_batch_size: 16
  learning_rate: 2.0e-5
  num_train_epochs: 2
  seed: 42
  train_subset: null

qnli:
  hf_dataset: glue
  hf_subset: qnli
  text_fields: [question, sentence]
  label_field: label
  num_labels: 2
  train_split: train
  eval_split: validation
  max_length: 128
  train_batch_size: 16
  eval_batch_size: 16
  learning_rate: 2.0e-5
  num_train_epochs: 2
  seed: 42
  train_subset: null

ag_news:
  hf_dataset: ag_news
  hf_subset: null
  text_fields: [text]
  label_field: label
  num_labels: 4
  train_split: train
  eval_split: test
  max_length: 128
  train_batch_size: 8
  eval_batch_size: 8
  learning_rate: 2.0e-5
  num_train_epochs: 1
  seed: 42
  train_subset: 20000
